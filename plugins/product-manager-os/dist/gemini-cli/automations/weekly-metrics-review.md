Run my weekly product review.

First, read `memory/MEMORY.md`, `memory/product.md`, and `memory/strategy.md` for our north-star metric, the metrics we watch, their targets, and this quarter's goals.

Then:
1. Use the `metrics-review` skill. Pull this week's numbers from a connected analytics tool. If none is connected and I haven't pasted numbers: when we're in a live chat, ask me for them; **if this is running unattended (scheduled/headless), don't wait — write a short note that live metrics weren't available and stop cleanly.** Never invent numbers.
2. Build the Metrics Scorecard: each metric with current vs prior vs target and a 🟢/🟡/🔴 status.
3. For every 🔴 or 🟡, give a hypothesis for what moved and why.
4. List 2–3 recommended actions with an owner.

Then use the `stakeholder-update` skill to draft a short **exec brief** version of this week's status that I can send to leadership — headline first, status color, one ask if there is one.

Show me both: the scorecard and the exec brief.
